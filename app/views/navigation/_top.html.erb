<nav class="fixed top-0 left-0 right-0 bg-slate-600 min-h-max">
  <span class="text-gold-600">bestClip</span>
  <ul class="list-none">
    <% if user_signed_in? %>
      <li><%= link_to "home", root_path %></li>
      <li><%= button_to "sign out", destroy_user_session_path, method: :delete %></li>
    <% else %>
      <li><%= link_to "google sign in", 
          user_google_oauth2_omniauth_authorize_path, 
          method: :post %>
      </li>
      <li><%= link_to "webapp sign in", new_user_session_path %></li>
    <% end %>
    <li><%= link_to "about", about_path %></li>
  </ul>
</nav>
